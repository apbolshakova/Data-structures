#include "Header.h"

func_result_t add(size_t i, char* data)
{
	//проверить индекс на корректность
	//если корректный, то
		//запомнить весь текст с индекса
		//remove(i, strlen(curText))
		//вставить с индекса i текст
		//вставить с индекса i + strlen(data) то, что было удалено
	//иначе ошибка
}